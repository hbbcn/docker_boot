# Docker Boot 项目问题总结

## 1. 编写接口向 user 表添加数据

**问题：** 需要编写一个接口向 `user` 表中添加两条数据。

**解决：**
- 在 `UserController` 中注入 `IUserService`。
- 使用 `@PostMapping("/addTwo")` 定义 POST 接口。
- 使用 `userService.saveBatch(users)` 批量保存数据。

**后续优化：** 接口改为接收前端参数 `@RequestBody List<User> users`，不再写死数据。

---

## 2. Spring Boot 2.7 集成 Swagger 启动报错 NullPointerException

**报错信息：**
```
Failed to start bean 'documentationPluginsBootstrapper'; nested exception is java.lang.NullPointerException
at springfox.documentation.spring.web.WebMvcPatternsRequestConditionWrapper.getPatterns
```

**原因：**
- Spring Boot 2.6+ 默认路径匹配策略从 `AntPathMatcher` 改为 `PathPatternParser`。
- Springfox 3.0.0 内部仍基于 `AntPathMatcher` 获取路径模式，两者不兼容导致 NPE。

**解决：**
在 `application.yaml` 中显式回退到旧的匹配策略：
```yaml
spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
```

**注意：** YAML 缩进必须正确，`mvc` 必须是 `spring` 的子节点。如果缩进错误（写到了 `server` 下），配置不会生效。

---

## 3. MyBatis-Plus 插入报错：argument type mismatch

**报错信息：**
```
Could not set property 'id' of 'class com.hbb.docker.entity.User' with value '2084265583783026690'
Cause: java.lang.IllegalArgumentException: argument type mismatch
```

**原因：**
- MyBatis-Plus 默认使用雪花算法（`ASSIGN_ID`）自动生成主键，生成的 ID 是一个很大的 `Long` 类型数值。
- `User` 实体类中的 `id` 字段是 `Integer` 类型，数值超出范围且类型不匹配。

**解决：**
1. 实体类 `id` 字段添加注解，使用数据库自增策略：
   ```java
   @TableId(type = IdType.AUTO)
   private Integer id;
   ```
2. 数据库 `user` 表的 `id` 字段设为**主键 + 自增**。

---

## 4. Docker 部署后访问 Swagger 报 404

**现象：** 本地 IDE 运行可以访问 Swagger，打成 jar 包在 Docker 里运行访问 `/swagger-ui/index.html` 报 404。

**原因：**
- IDE 本地以 exploded 方式运行，`META-INF/resources/webjars/` 下的静态资源能被自动扫描。
- 打成 jar 包后，Springfox 3.0.0 在部分环境下不会自动注册资源处理器，导致静态资源无法暴露。

**解决：**
添加配置类手动映射 Swagger UI 资源路径：
```java
@Configuration
public class WebMvcConfig implements WebMvcConfigurer {
    @Override
    public void addResourceHandlers(ResourceHandlerRegistry registry) {
        registry.addResourceHandler("/swagger-ui/**")
                .addResourceLocations("classpath:/META-INF/resources/webjars/springfox-swagger-ui/");
        registry.addResourceHandler("/webjars/**")
                .addResourceLocations("classpath:/META-INF/resources/webjars/");
    }
}
```

---

## 5. docker-compose 相关 issues

### 5.1 docker-compose 命令报错 `syntax error near unexpected token newline`

**原因：** `/usr/local/bin/docker-compose` 文件内容是一个 HTML 页面（下载时出错），不是可执行程序。

**解决：**
- 推荐直接使用 Docker 自带的命令（无需单独安装）：
  ```bash
  docker compose up -d
  ```
- 或重新下载正确的 docker-compose 二进制文件。

### 5.2 Volume 挂载：文件 vs 目录

**规则：**
| 类型 | 是否需提前创建 | 说明 |
|------|---------------|------|
| 目录挂载 | 不需要 | 不存在时 Docker 自动创建为空目录 |
| 文件挂载 | **必须提前创建为文件** | 不存在时 Docker 会创建为**目录**，导致容器内进程读取失败 |

**案例：**
```yaml
volumes:
  - /app/mysql/conf/my.cnf:/etc/my.cnf   # 文件挂载，必须提前存在
  - /app/mysql/db:/var/lib/mysql         # 目录挂载，自动创建
```

如果 `/app/mysql/conf/my.cnf` 不存在，Docker 会创建成一个**目录**。MySQL 启动时发现 `/etc/my.cnf` 是目录就会跳过，使用镜像默认配置启动，所以容器能跑起来，但自定义配置（如字符集）不会生效。

### 5.3 MySQL 不能存中文

**原因：** MySQL 5.7 镜像默认字符集通常是 `latin1`。当配置文件挂载失败（被创建成目录）时，MySQL 使用默认配置启动，导致无法存储中文。

**解决：**
1. 停止并删除容器
2. 删除被错误创建成目录的 `my.cnf`：`rm -rf /app/mysql/conf/my.cnf`
3. 重新创建为文件并写入 utf8mb4 配置：
   ```bash
   mkdir -p /app/mysql/conf
   cat > /app/mysql/conf/my.cnf << 'EOF'
   [mysqld]
   character-set-server=utf8mb4
   collation-server=utf8mb4_unicode_ci
   init_connect='SET NAMES utf8mb4'

   [client]
   default-character-set=utf8mb4

   [mysql]
   default-character-set=utf8mb4
   EOF
   ```
4. 重新启动容器

---

## 6. 缓存逻辑：添加数据时先写 Redis，查询时先查 Redis

**实现：**
- **添加接口：** 先保存到 MySQL，获取自增 ID 后，再将数据以 JSON 格式写入 Redis（key: `user:id`，设置过期时间）。
- **查询接口：** 先查 Redis，命中直接返回；未命中则查 MySQL，并将结果写入 Redis 后再返回。

**依赖：**
- `pom.xml` 引入 `spring-boot-starter-data-redis`
- `application.yaml` 配置 Redis 连接信息
- 使用 `StringRedisTemplate` + Hutool `JSONUtil` 进行序列化

---

## 7. Swagger 访问地址

Springfox 3.0.0 的默认访问地址：
- **Swagger UI：** `http://IP:6001/swagger-ui/index.html`
- **API 文档 JSON：** `http://IP:6001/v2/api-docs`